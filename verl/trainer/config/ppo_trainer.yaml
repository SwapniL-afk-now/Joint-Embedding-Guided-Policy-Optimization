# Format checks enforced on CI:
# 1. Comments must appear above each field.
# 2. There must be a blank line between each field.
# 3. Inline comments (after a field on the same line) are not allowed.
# 4. Indentation level is respected for nested fields.

# specify the default per-component configs
defaults:

  - model_engine: dp

  # <folder_name>@<field_name>.<field_name>: <yaml_file_name>
  # actor_rollout_ref.actor: trainer/config/actor/dp_actor.yaml
  - actor@actor_rollout_ref.actor: ${model_engine}_actor

  # data: trainer/config/data/legacy_data.yaml
  - data@data: legacy_data

  # Reference model config.
  # Reference model will be enabled when actor.use_kl_loss or/and algorithm.use_kl_in_reward is/are True.
  - ref@actor_rollout_ref.ref: ${model_engine}_ref

  # Rollout model config.
  - rollout@actor_rollout_ref.rollout: rollout

  # Model config.
  - model@actor_rollout_ref.model: hf_model

  # Critic model config.
  - critic@critic: ${model_engine}_critic
  - model@critic.model: hf_model

  # legacy reward impl config, for backward compatibility
  - legacy_reward_impl

  # Reward config.
  - reward@reward: reward

  # Rollout correction config.
  - algorithm@algorithm.rollout_correction: rollout_correction

  # distillation config
  - distillation@distillation: distillation

  # load the reference default config, then apply the fields in the current yaml
  # self config override anything above
  - _self_

# config for actor, rollout and reference model
actor_rollout_ref:

  # Whether it's a hybrid engine, currently only supports hybrid engine
  hybrid_engine: true

  # Timeout for operations executed against the process group
  nccl_timeout: 600

# config for the algorithm
algorithm:

  # Required when using verl.utils.omega_conf_to_dataclass to instantiate dataclass configs
  _target_: verl.trainer.config.AlgoConfig

  # Discount factor for future rewards
  gamma: 1.0

  # Trade-off between bias and variance in the GAE estimator
  lam: 1.0

  # Advantage estimator type: "gae", "grpo", "reinforce_plus_plus", etc.
  adv_estimator: gae
  base_rl_name: null

  # Whether to normalize advantages by std (specific to GRPO)
  norm_adv_by_std_in_grpo: True
  ngrpo_r_max: 1.0
  ngrpo_epsilon_pos: 0.24
  ngrpo_epsilon_neg: 0.16
  avspo_collapse_tau: 1.0e-6
  avspo_alpha: 0.5
  avspo_anchor_reward: 0.1
  avspo_adapt_tau_init: 0.5
  avspo_adapt_eta: 0.01

  # Whether to enable in-reward KL penalty
  use_kl_in_reward: False

  # How to estimate KL divergence: "kl", "abs", "mse", "low_var_kl", or "full"
  kl_penalty: kl

  # KL control configuration
  kl_ctrl:

    # Required when using verl.utils.omega_conf_to_dataclass to instantiate dataclass configs
    _target_: verl.trainer.config.KLControlConfig

    # KL control type: "fixed" or "adaptive"
    type: fixed

    # Initial coefficient for KL penalty
    kl_coef: 0.001

    # Horizon value for adaptive controller (if enabled)
    horizon: 10000

    # Target KL divergence (used for adaptive controller)
    target_kl: 0.1

  # Whether to enable preference feedback PPO
  use_pf_ppo: False

  # Preference feedback PPO settings
  pf_ppo:

    # Method for reweighting samples: "pow", "max_min", or "max_random"
    reweight_method: pow

    # Power used for weight scaling in "pow" method
    weight_pow: 2.0

# config for Group Policy-Improvement Cross Entropy (GPI-CE)
custom_gpi_ce:

  # Whether to enable the GPI-CE objective (replaces advantage estimation entirely)
  enable: false

  # Online candidates per prompt; MUST equal actor_rollout_ref.rollout.n
  online_per_prompt: 4

  # Offline candidates appended per prompt after the rollout replicas sleep
  offline_per_prompt: 4

  # tau in q* = softmax(s_old + r/tau); 0.5 for binary rewards
  temperature: 0.5

  # Sequence score reduction; only token_mean is implemented
  score_normalization: token_mean

  # Score offline candidates with the training verifier instead of trusting the parquet
  verify_offline: true

  # Cycle available offline responses when a prompt has fewer than offline_per_prompt
  allow_short_offline: false

  # Build q* inside the training forward instead of running a separate old-policy
  # forward. Exact when ppo_epochs=1 and there is one optimizer step per batch.
  fuse_old_log_prob: true

# config for Success/Failure Distillation Contrastive GRPO
custom_sdc:
  enable: false
  beta: 0.5
  scoring_backend: hf
  vllm_score_micro_batch_size: 16
  reuse_rollout_log_probs: false
  verify_rollout_log_probs: false
  use_importance_weight: true
  importance_weight_clip: 10.0
  base_mix_gamma: 0.9
  sft_update_interval_policy_steps: 5
  checkpoint_interval_policy_steps: 10
  min_sft_updates_before_use: 1
  sft_lr: 1.0e-6
  sft_batch_size: 8
  sft_max_token_len_per_gpu: 0
  sft_max_updates_per_interval: 1
  data_max_size: null
  data_sampling: recent
  save_to_disk_interval_policy_steps: 0
  loss_agg_mode: token-mean

# config for Sequence Sharpening GRPO
custom_sharpening_grpo:

  # Whether to enable the Sequence Sharpening actor loss
  enable: false

  # Toggle the standard GRPO reward advantage loss. When false, the GRPO
  # contribution to the total actor loss is exactly 0 (verifier-free ablation).
  use_grpo_reward: true

  # Weight on the entropy-rate / GRSA sequence sharpening term
  gamma: 0.1

  # Auxiliary scale on the sequence sharpening term; combined as gamma*alpha
  alpha: 1.0

  # Coefficient for the K3 KL penalty (exp(r) - 1 - r) toward the reference
  beta: 0.1

  # Group size G for GRSA -- must equal actor_rollout_ref.rollout.n
  group_size: 8

  # PPO clip epsilon for the sequence sharpening surrogate
  clip_ratio: 0.2

# config for abstract-reasoning compression GRPO
custom_abstract_rl:

  # Whether to enable the abstraction utility reward + compression KL
  enable: false

  # false -> U_i = r_i (compression-only ablation)
  use_utility: true

  # lambda on the abstraction compression KL; 0 -> usefulness-only ablation
  kl_coef: 0.05

  # verl KL estimator for the compression term ("low_var_kl" is k3)
  kl_type: low_var_kl
  kl_clip: 2.0

  # clip bound on the per-token likelihood gain Delta
  delta_clip: 1.0
  invalid_delta: zero
  valid_bonus: 0.2

  # "matched": baseline scored under the same template with an empty abstract
  # "stored" : spec-exact, baseline read from the rollout old_log_probs
  delta_baseline: matched

  # true -> feed 2U-1 (identical to the +-1 task reward when Delta == 0)
  utility_affine: true

  # abstraction delimiters
  open_tag: "<abstract>"
  close_tag: "</abstract>"

  # only the last N response tokens are scanned for the tags
  max_scan_tokens: 512

  # hard cap on |A_i|; over budget -> invalid (v_i=0, same as missing/degenerate).
  # 0 disables. Fixes the reward hack where the model dumped the whole solution
  # into <abstract> -- see plan.md section 15.
  max_abstract_tokens: 120

  # beta: subtracted from U_i as beta * |A_i| / max_abstract_tokens, on valid rows.
  # 0 disables (default, for backward-compatible test/CLI behavior).
  length_penalty_coef: 0.0

  # append the abstraction instruction to the rollout prompt (train and val)
  inject_instruction: true

  # "spec" is the instruction exactly as specified; "strong" states the same
  # requirement imperatively and measures a ~7x higher emission rate
  instruction_variant: spec

  # free-form instruction; wins over instruction_variant when non-empty
  instruction_override: ""

  # "user_turn" appends to the last user message; "system" inserts/extends a
  # dedicated system message instead
  instruction_placement: user_turn

  # true -> only correct rollouts enter the scoring batch (~2x cheaper)
  score_only_correct: false

  # token budget per GPU for the teacher-forced scoring forward
  scoring_max_token_len_per_gpu: 24576

  # cap on reasoning tokens re-scored per row (0 = no cap)
  max_score_response_tokens: 0

  # "trailing": append-to-the-tail graft (solution already known, target sits
  # after Y). "fill_gap": pass 1 already asks for abstract+solution together; a
  # missing/degenerate abstract gets a question-only pass-2 fill and the row is
  # reassembled abstract-first regardless of where pass 1 put it.
  bootstrap_mode: trailing

  # two-phase format bootstrap: graft abstractions into rollouts that emitted none,
  # for the first N steps. 0 disables it. This is a SECOND generation stage and makes
  # the update a reward-weighted self-distillation until it latches off.
  bootstrap_steps: 0

  # fraction of eligible rollouts to graft; must be strictly between 0 and 1, or the
  # gradient on emitting the tags cancels within the group
  bootstrap_graft_fraction: 0.5

  # latch grafting off once the native emission rate holds at/above this...
  bootstrap_native_threshold: 0.3

  # ...for this many consecutive steps
  bootstrap_patience: 3

  # token cap on a grafted abstraction block
  bootstrap_max_abstract_tokens: 256

  # for the first N steps replace the policy loss with a plain SFT loss on the
  # abstraction span of correct, valid rollouts (the bootstrap update is not a
  # policy gradient anyway). Must be <= bootstrap_steps. 0 disables.
  bootstrap_sft_steps: 0
  bootstrap_sft_lr: 0.0
  bootstrap_sft_clip_grad: 0.0
  bootstrap_sft_accuracy_drop: 0.10
  bootstrap_sft_min_length_fraction: 0.6
  bootstrap_abstract_cache: ''

  # print N parsed examples per step (smoke runs)
  debug_print_n: 0

# config for the trainer
trainer:

  # Whether to balance batch sizes across distributed workers
  balance_batch: True

  # Number of epochs in training
  total_epochs: 30

  # Total training steps (can be set explicitly or derived from epochs)
  total_training_steps: null

  # Project name for experiment tracking (e.g., wandb)
  project_name: verl_examples

  # Experiment name for run identification in tracking tools
  experiment_name: gsm8k

  # Logging backends to use: "console", "wandb", etc.
  logger: ["console", "wandb"]

  # Number of generations to log during validation
  log_val_generations: 0

  # Directory for logging rollout data; no dump if null
  rollout_data_dir: null

  # Directory for logging validation data; no dump if null
  validation_data_dir: null

  # Number of nodes used in the training
  nnodes: 1

  # Number of GPUs per node
  n_gpus_per_node: 8

  # Save frequency (by iteration) for model checkpoints
  save_freq: -1

  # ESI refers to the elastic server instance used during training, similar to the training plan. For example,
  # if you purchase 10 hours of computing power, the ESI will automatically shut down after 10 hours of training.
  # To ensure a checkpoint is saved before ESI shuts down, the system will start saving a checkpoint in advance.
  # The advance time is calculated as: Advance Time = Longest historical step duration + Checkpoint save duration + esi_redundant_time.
  # Here, esi_redundant_time is a user-defined value that further extends the advance time for added safety.
  esi_redundant_time: 0

  # Resume mode: "auto", "disable", or "resume_path"
  # "auto": resume from last checkpoint if available
  # "disable": start from scratch
  # "resume_path": resume from a user-defined path
  resume_mode: auto

  # Path to resume training from (only used when resume_mode is "resume_path")
  resume_from_path: null

  # Whether to run validation before training begins
  val_before_train: True

  # Whether to run validation only
  val_only: False

  # Validation frequency (in training iterations)
  test_freq: -1

  # Number of iterations to warm up the critic before updating policy
  critic_warmup: 0

  # Default path to distributed filesystem for saving checkpoints
  default_hdfs_dir: null

  # Whether to delete local checkpoints after loading
  del_local_ckpt_after_load: False

  # Default local directory for saving checkpoints
  default_local_dir: checkpoints/${trainer.project_name}/${trainer.experiment_name}

  # Maximum number of actor checkpoints to keep
  max_actor_ckpt_to_keep: null

  # Maximum number of critic checkpoints to keep
  max_critic_ckpt_to_keep: null

  # Timeout (in seconds) for Ray worker to wait for registration
  ray_wait_register_center_timeout: 300

  # Device to run training on (e.g., "cuda", "cpu")
  device: cuda

# profiler configs
global_profiler:

  # Required when using verl.utils.omega_conf_to_dataclass to instantiate dataclass configs
  _target_: verl.utils.profiler.ProfilerConfig

  # Profiling tool: choose between nsys, npu, torch, torch_memory, precision_debugger
  tool: null

  # profile steps
  steps: null

  # Whether to combine continuous steps into one database.
  ## If True, worker.profiler.discrete must be False, [1,2] in one, [5] in another.
  ## If False, [1] in one, [2] in another, [5] in another.
  profile_continuous_steps: False

  # Path to save profiling contents
  save_path: "outputs/profile"

  # Specific tool configs, can use +profiler.tool_config.[tool].xxx to config
  global_tool_config:

    # nsys config
    nsys:

      # Required when using verl.utils.omega_conf_to_dataclass to instantiate dataclass configs
      _target_: verl.utils.profiler.config.NsightToolConfig

      # True for each task has its own database, False for all tasks in one training step share one database.
      discrete: False

      # controller Nvidia Nsight Systems Options. Must set when profile_steps is not None.
      ## reference https://docs.nvidia.com/nsight-systems/UserGuide/index.html
      ## reference https://docs.ray.io/en/latest/ray-observability/user-guides/profiling.html
      controller_nsight_options:

        # Select the API(s) to be traced.
        trace: "cuda,nvtx,cublas,ucx"

        # Track the GPU memory usage by CUDA kernels. Must be string type "true" or "false".
        cuda-memory-usage: "true"

        # CUDA graphs will be traced as a whole
        cuda-graph-trace: "graph"

      # worker Nvidia Nsight Systems Options. Must set when profile_steps is not None.
      worker_nsight_options:

        # Select the API(s) to be traced.
        trace: "cuda,nvtx,cublas,ucx"

        # Track the GPU memory usage by CUDA kernels. Must be string type "true" or "false".
        cuda-memory-usage: "true"

        # CUDA graphs will be traced as a whole
        cuda-graph-trace: "graph"

        # Profiling only in a range of torch.cuda.profiler.start and stop. Do not change this config.
        capture-range: "cudaProfilerApi"

        # Specify the desired behavior when a capture range ends.
        # In verl we need the torch.cuda.profiler.start/stop pair to repeats n times.
        # valid values are "repeat-shutdown:n" or null.
        # For normal whole step profiling, n = len(profile_steps);
        # but for discrete profiling, n = len(profile_steps) * Number(subtasks).
        # Or you can just leave it null and the program will use n = len(profile_steps) * 6;
        capture-range-end: null

        # Send signal to the target application's process group. We let the program to exit by itself.
        kill: none

    # enable memory visualization for debugging memory usage
    torch_memory:

      #  Maximum number of allocation entries to record
      trace_alloc_max_entries: 100_000

      # The depth of the call stack to capture for each allocation
      stack_depth: 32

      # 'alloc': records only allocation events || 'state': records memory state changes || 'all': records both.
      context: "all"

      # 'python': records Python stacks || 'cpp': records C++ stacks (available in some versions) || 'all': records both.
      stacks: "all"

      # devices, record_context etc.
      kw_args: {}

    # msprobe precision debugger
    precision_debugger:

      # Required when using verl.utils.omega_conf_to_dataclass to instantiate dataclass configs
      _target_: verl.utils.profiler.config.PrecisionDebuggerToolConfig

      # Path to msprobe config json
      config_path: null

      # Deprecated and ignored. Use global_profiler.steps.
      steps: null

      # Profile stages
      # Supported stages: actor_update, actor_compute_log_prob, ref_compute_log_prob,
      # compute_values, critic_update, compute_rm_score
      stages: null

      # Whether to fail on unknown stage or missing msprobe
      strict: False

# configs for TransferQueue
transfer_queue:

  # Whether to enable TransferQueue
  enable: False

  # Pluggable storage/transport backend of TransferQueue
  backend:

    # SimpleStorage with ZeroMQ
    storage_backend: SimpleStorage

    # Backend specific configs
    SimpleStorage:
      
        # Maximum number of samples to hold
        total_storage_size: 100000

        # Number of distributed storage units
        num_data_storage_units: 8

# configs related to ray
ray_kwargs:

  # configs related to ray initialization
  ray_init:

    # Number of CPUs for Ray. Use a fixed number instead of null when using SLURM.
    num_cpus: null

  # Path to save Ray timeline JSON for performance profiling
  timeline_json_file: null
  loss_agg_mode: token-mean
